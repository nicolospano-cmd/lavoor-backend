{
  "name": "lavoor-backend",
  "version": "1.0.0",
  "description": "Lavoor demo backend (json-server)",
  "main": "index.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port 10000 --watch db.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
